Convert office documents (DOCX, XLSX, PDF, PPTX) to plain text for Git diff visualization.

## Features
- Convert DOCX, XLSX, PDF, PPTX files to plain text
- Integrate with Git for visual diffing
- Support for multiple formats with fallback converters
- Virtual environment setup for dependency isolation
